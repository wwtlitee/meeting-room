# REPORT_activation_notice_v1.8.0

## 基本信息

- 日期：2026-05-29
- 版本：v1.8.0
- 负责人：Solazhu
- 范围：专家团 skill 触发确认提示、用户可见启动流程

## 修改内容

- `SKILL.md` 新增 Activation notice hard rule：一旦 `agency-agents` 被触发，第一可见句必须固定输出 `agency agents 会议skill已触发～喵`。
- `Function 1 must` 流程第 1 步改为先输出触发提示，再进入会议模式选择、会议页启动或其他流程。
- `GUIDE_专家团skill接手引导.md` 同步 v1.8.0：触发提示必须早于会议模式选择和会议页启动。
- `pdoc/log/memory.md` 记录本次触发提示规则变更。

## 影响范围

- 影响所有专家团/会议 skill 触发场景，用户会先看到明确的 skill 命中提示。
- 不改变会议内容生成、右侧浏览器启动、禁止兜底和会后执行选择规则。

## 风险与防护

- 风险：模型可能把提示语改写或延后输出。
- 防护：规则要求第一可见句必须使用固定文本，不允许改写、忽略或延迟。

## 复核结果

- 已静态检查 `SKILL.md` 包含固定触发提示和 Function 1 首步要求。
- 待用户产品侧验证：再次输入“专家团会议 / 会议skill / 开个会”时，第一句应显示固定触发提示。
